import React from "react";

import "./estilos/seccionEditor.scss";

const SeccionEditor = (props) => {
  return (
    <div>
      <p>
        Holiwuie, soy un componente que no esta linkeado en la barra de
        navegacion y en genera en ningun lado porque soy nuevo
      </p>
    </div>
  );
};

export default SeccionEditor;
